import {Typography} from '@mui/material';

const TitleBar: React.FC = () => {
  return (
    <Typography data-cy="title" variant="h6">Craft Manager</Typography>
  );
};

export default TitleBar;